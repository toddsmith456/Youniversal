# Youniversal is a pure Compose design system: no reflection, no JNI, no serialization.
# Compose's own consumer rules already keep what the runtime needs, so nothing is required here.
#
# If you add reflection-based features (for example a JSON-backed theme), add the matching
# -keep rules to this file so they reach every consuming app.
